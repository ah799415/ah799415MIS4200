@model IEnumerable<ah799415MIS4200.Models.Vets>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.vetDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VetCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vetDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VetCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.vetsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.vetsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.vetsID })
        </td>
    </tr>
}

</table>
