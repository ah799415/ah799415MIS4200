@model IEnumerable<ah799415MIS4200.Models.Visits>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pet.petFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vets.vetDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visitDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visitDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pet.petFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vets.vetDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visitDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visitDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.visitNum }) |
            @Html.ActionLink("Details", "Details", new { id=item.visitNum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.visitNum })
        </td>
    </tr>
}

</table>
